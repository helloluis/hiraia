# Expansion run outputs (tracked)

Per-agent JSONL from the parallel generator wave, committed as each agent
completes so an ephemeral-container recycle cannot lose work. Merged into
`../../bank/science-facts.jsonl` by `../ingest-expansion.py` at the end of the wave.
