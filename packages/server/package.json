{
  "name": "@hiraia/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local QVAC inference sidecar for the Hiraia web demo. Exposes an OpenAI-compatible /v1/chat/completions SSE endpoint backed by @qvac/sdk (Qwen3-1.7B Q4 via llama.cpp).",
  "main": "src/index.mjs",
  "scripts": {
    "dev": "node src/index.mjs",
    "start": "node src/index.mjs",
    "smoke": "node src/smoke.mjs",
    "train": "node src/train.mjs",
    "train:validate": "node src/train.mjs --quick --device cpu"
  },
  "dependencies": {
    "@qvac/sdk": "^0.17.1"
  }
}
