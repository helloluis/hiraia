# Full second pass: 12,245 original remaining facts

User authorized the full remaining inventory after committing the first pass. This is a new run, not an extension of the expired/completed original heartbeat. Inventory is immutable second-pass-inventory.json; assignment/progress state is second-pass-state.json. The original checkpoint and first-pass heartbeat history remain preserved. Cumulative checkpoint includes both passes after integration, while second-pass state records only this run.

Four runtime slots: three dedicated reviewers plus parent. Reuse recovery_a/b/c; parent reviews a small fourth packet (12–24) between integrations. Never spawn more than three child reviewers or duplicate active assignments. Parent alone exports, applies, regenerates, integrates, updates shared state. Reviewers own only assigned numbered JSONL and notes. No paid external-model API calls, commits, builds, deployment or push during this run unless separately requested.

Use second-pass-batch.py --limit 96 --out tools/curriculum-recovery/batch-NNN.packet.json. It reserves IDs across ALL packets/reviews, prioritizes wrong prior assignments, and searches every authored competency. Global regex matches are noisy leads, not proof. Search exact competencies across all grades and prefer scarce/distinct teaching components; no blanket keyword remapping or loosening of all filters. Check full English and TL/BIS for approvals, both pools and every stable fact/card-ID variant, every live facet the chosen code would admit, source quality and age appropriateness. Useful teaching components need not be entire activities, but never award diagram/table activity credit falsely. Hold unresolved content/translation/duplicate/filter problems with precise reasons and potential destination, without marking unseen facts reviewed.

Keep the existing review schema, stable ID/hash/exclusion and explicit code/unit/quote evidence. Save complete packets atomically. Dry-run apply.py only for children. Parent verifies packet completeness, identity, disjointness and every proposed mapping before sequential apply with rollback. If compiler picks an alternate identity, reconcile explicitly or hold; never weaken exact membership tests. Overrides are nested in curriculumTagOverrides.json under factoids.

Before integration snapshot all eight manifests. Regenerate tags and grades3–10, audit content reach, assert no previous cardIds/relatedCardIds lost. Run core-recovery, all grade lesson, calendar-selection, curriculum-default, review-series, feed-recap, subcategory-regression and review-persistence tests, plus mobile type-check. Update cumulative checkpoint before tests (approved count), then finalize state/reports only after success. Count only applied validated decisions as recoveries. Pending complete reviews remain pending.

After each wake integrate ready batches before redispatch where practical, then checkpoint and report this-run and cumulative counts. When waiting on children, finish the turn; use completion notifications, no idle polling. Parent packet should finish in that turn when possible. Keep at least one assignment available for each idle reviewer until inventory exhausted. Stop the new heartbeat when all12,245 decisions are integrated/validated, or at its configured expiry; save actual remaining work without silently extending.

## Four-way partition update

User assigned three other agents separate sets. Always honor second-pass-partitions.json. Local exporter defaults to local only. External A/B/C use packet ranges10000–19999/20000–29999/30000–39999 and own separate ledgers; never dispatch or overwrite them locally. See docs/SECOND-PASS-PARALLEL-HANDOFF.md and docs/RECOVERY-SET-{A,B,C}.md. Local coordinator integrates ready complete external reviews centrally after the same validations, updates cumulative/second-pass counts and a separate integration ledger without changing externally owned assignment ledgers. Do not stop the overall heartbeat merely because local partition is finished: distinguish external work still pending, then checkpoint if no integration is ready.
